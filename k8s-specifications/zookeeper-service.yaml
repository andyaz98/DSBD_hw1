apiVersion: v1
kind: Service
metadata:
  labels:
    app: zookeeper
  name: zookeeper
spec:
  type: ClusterIP
  ports:
  - name: "zookeeper-service"
    port: 2181
    targetPort: 2181
  selector:
    app: zookeeper

