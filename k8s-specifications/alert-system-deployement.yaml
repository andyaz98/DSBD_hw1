apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: alert-system
  name: alert-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-system
  template:
    metadata:
      labels:
        app: alert-system
    spec:
      containers:
      - image: andyaz/alert-system:latest
        name: alert-system

