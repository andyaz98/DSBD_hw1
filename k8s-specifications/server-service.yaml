apiVersion: v1
kind: Service
metadata:
  labels:
    app: server
  name: server
spec:
  type: NodePort
  ports:
  - name: "server-service"
    port: 50051
    targetPort: 50051
    nodePort: 30001
  selector:
    app: server

