apiVersion: v1
kind: Service
metadata:
  labels:
    app: broker-1
  name: broker-1
spec:
  type: ClusterIP
  ports:
  - name: "broker-1-service"
    port: 9092
    targetPort: 9092
  selector:
    app: broker-1

