apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus
  name: prometheus
spec:
  type: NodePort
  ports:
  - name: "prometheus-service"
    port: 9090
    targetPort: 9090
    nodePort: 30002
  selector:
    app: prometheus

